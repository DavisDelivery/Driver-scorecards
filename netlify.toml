[build]
  command = "vite build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback — let the React client handle client-side routing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
